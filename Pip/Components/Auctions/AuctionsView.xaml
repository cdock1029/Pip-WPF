<UserControl x:Class="Pip.UI.Components.Auctions.AuctionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
             xmlns:view="clr-namespace:Pip.UI.View"
             xmlns:auctions="clr-namespace:Pip.UI.Components.Auctions"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type={x:Type auctions:AuctionsViewModel}}"
             d:DesignHeight="450" d:DesignWidth="800">

    <!--Loaded="{DXEvent Handler='LoadUpcoming()'}"-->
    <Grid>
        <dxdo:DockLayoutManager>
            <dxdo:LayoutGroup Orientation="Vertical">
                <dxdo:DocumentGroup Caption="Auctions"
                                    ShowCaption="True"
                                    SelectedTabIndex="0"
                                    SelectedItemChanged="{DXEvent Handler='HandleSelectedItem(@args)'}">
                    <dxdo:DocumentPanel Caption="Recent" ShowBorder="False" x:Name="RecentTab">
                        <view:TreasuriesCollectionView
                            Loaded="{DXEvent Handler='LoadRecent()'}"
                            HandleSelectionChangedCommand="{Binding DetailsViewModel.HandleSelectionChangedCommand, Mode=OneWay}"
                            SelectedTreasury="{Binding SelectedTreasuryRecent, Mode=TwoWay}"
                            Treasuries="{Binding TreasuriesRecent}" />
                    </dxdo:DocumentPanel>

                    <dxdo:DocumentPanel Caption="Upcoming" ShowBorder="False" x:Name="UpcomingTab">
                        <view:TreasuriesCollectionView
                            Loaded="{DXEvent Handler='LoadUpcoming()'}"
                            HandleSelectionChangedCommand="{Binding DetailsViewModel.HandleSelectionChangedCommand, Mode=OneWay}"
                            SelectedTreasury="{Binding  SelectedTreasuryUpcoming}"
                            Treasuries="{Binding  TreasuriesUpcoming}" />
                    </dxdo:DocumentPanel>
                </dxdo:DocumentGroup>
            </dxdo:LayoutGroup>
        </dxdo:DockLayoutManager>
    </Grid>
</UserControl>