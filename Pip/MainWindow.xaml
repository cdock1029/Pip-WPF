<Window x:Class="Pip.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:viewModel="clr-namespace:Pip.UI.ViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModel:MainViewModel}"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow"
        Height="800" Width="600"
        FontSize="16">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Menu>
            <MenuItem Header="_File">
                <MenuItem Header="_Quit">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:CallMethodAction MethodName="Close"
                                                TargetObject="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_About" />
        </Menu>

        <!-- Toolbar -->
        <DockPanel Grid.Row="1">
            <ToolBarTray DockPanel.Dock="Top">
                <ToolBar>
                    <Button Content="Search" Command="{Binding NavigateSearchCommand}" />
                    <Button Content="Saved Treasuries" Command="{Binding NavigateSavedCommand}" />
                    <Button Content="Upcoming Auctions" Command="{Binding NavigateUpcomingCommand}" />
                    <Button Content="Auctions" Command="{Binding NavigateAuctionsCommand}" />
                </ToolBar>
            </ToolBarTray>
        </DockPanel>

        <!-- Content -->
        <ContentControl Grid.Row="2" Margin="20" Content="{Binding Navigation.CurrentView}" />

        <StatusBar Grid.Row="3">
            <Label>Pip a rino</Label>
        </StatusBar>
    </Grid>
</Window>
