<UserControl x:Class="Pip.UI.View.SavedTreasuriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:Pip.UI.ViewModel"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModel:SavedTreasuriesViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">


    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <!-- Content -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!-- Treasuries list -->
        <ListView Grid.Column="0"
                  MinWidth="200"
                  SelectedItem="{Binding SelectedTreasury}"
                  ItemsSource="{Binding Treasuries}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" />
                    <GridViewColumn Header="Cusip" DisplayMemberBinding="{Binding Cusip}" />
                    <GridViewColumn Header="Issue Date" DisplayMemberBinding="{Binding IssueDate}" />
                    <GridViewColumn Header="Maturity Date" DisplayMemberBinding="{Binding MaturityDate}" />
                </GridView>
            </ListView.View>
        </ListView>

        <UniformGrid Grid.Column="1" Margin="10 0" Rows="3" Columns="1">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" VerticalAlignment="Top">
                <Label FontWeight="SemiBold" Margin="0 0 5 0" VerticalAlignment="Center">Cusip:</Label>
                <TextBlock Text="{Binding SelectedTreasury.Cusip, FallbackValue=''}" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" VerticalAlignment="Top">
                <Label FontWeight="SemiBold" Margin="0 0 5 0" VerticalAlignment="Center">Issue Date:</Label>
                <TextBlock Text="{Binding SelectedTreasury.IssueDate, FallbackValue=''}" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" VerticalAlignment="Top">
                <Label FontWeight="SemiBold" Margin="0 0 5 0" VerticalAlignment="Center">Maturity Date:</Label>
                <TextBlock Text="{Binding SelectedTreasury.MaturityDate, FallbackValue=''}" VerticalAlignment="Center" />
            </StackPanel>
        </UniformGrid>

    </Grid>
</UserControl>
