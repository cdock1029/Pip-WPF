<UserControl x:Class="Pip.UI.View.TreasuriesCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False"
                  ItemsSource="{Binding Treasuries}"
                  SelectedItem="{Binding SelectedTreasury}" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="Auto" IsReadOnly="True" />
                <DataGridTextColumn Header="Term" Binding="{Binding  SecurityTerm}" Width="Auto" IsReadOnly="True" />
                <DataGridTextColumn Header="Cusip" Binding="{Binding Cusip}" Width="Auto" IsReadOnly="True" />
                <DataGridTextColumn Header="Issue Date" Binding="{Binding IssueDate}" Width="Auto" IsReadOnly="True" />
                <DataGridTextColumn Header="Maturity Date" Binding="{Binding MaturityDate}" Width="Auto"
                                    IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
